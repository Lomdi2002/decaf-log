# CLAUDE.md

# Decaf Log - Claude Code 開発ルール

## 1. プロジェクトの目的

Decaf Logは、日々のカフェイン摂取量を記録するWebアプリケーションである。

本プロジェクトの最優先目標は以下とする。

**安定して動作するアプリを完成させ、テストし、デプロイすること。**

優先順位は以下とする。

1. 正しく動作すること
2. 完成させること
3. シンプルであること
4. 安定していること
5. 保守しやすいこと
6. ユーザー体験が良いこと

不必要に複雑な実装より、分かりやすく直接的な実装を優先する。

---

## 2. 仕様の優先順位

機能を実装・変更する前に、関連するプロジェクトドキュメントを確認すること。

主要ドキュメント：

- `docs/requirements.md`
- `docs/screen-design.md`

仕様判断の優先順位は以下とする。

1. ユーザーからの最新の明示的な指示
2. `docs/requirements.md`
3. `docs/screen-design.md`
4. 現在の実装
5. その他のドキュメント

各ドキュメントの役割：

- `requirements.md`
  - アプリが「何をするか」を定義する
- `screen-design.md`
  - 必要な機能を「どのように画面へ表示するか」を定義する
- `CLAUDE.md`
  - 開発作業を「どのように進めるか」を定義する

仕様間に矛盾がある場合や、重要な要件が曖昧な場合は以下を守る。

- 勝手に新しい仕様を作らない
- 現在の要件を満たす最もシンプルな解釈を優先する
- 重要な製品判断を行う前に、曖昧な点を説明する

---

## 3. 現在の開発優先順位

現在の最優先事項は、

**Version 1.0 MVPの完成**

である。

MVPの機能要件は以下で定義されている。

`docs/requirements.md`

ユーザーから明示的に指示されない限り、MVP後の機能を実装しないこと。

MVPでは、以下のような機能を勝手に追加しない。

- カフェイン目標設定
- プログレスバー
- グラフ
- 連続達成日数
- 飲み物プリセット
- 記録編集
- 通知
- SNS機能
- AIアドバイス
- 高度な認証
- ダークモード
- 不要なアニメーション

「便利そうだから」という理由で、プロジェクトのスコープを広げないこと。

---

## 4. 技術スタック

ユーザーから明示的に変更指示がない限り、以下の技術を使用する。

### フロントエンド

- React
- JavaScript
- Vite

### ルーティング

- React Router

### スタイリング

- 通常のCSS

### バックエンド / データベース

- Supabase
- PostgreSQL

### テスト

- Vitest
- React Testing Library

### バージョン管理

- Git
- GitHub

### CI

- GitHub Actions

### デプロイ

- Vercel

明示的な指示がない限り、以下へ移行しない。

- TypeScript
- Next.js
- Tailwind CSS
- Redux
- Zustand
- 他のデータベース
- 他のフレームワーク

既存技術で十分実装できる場合、利便性だけを理由に不要なライブラリを追加しない。

---

## 5. 開発方針

要件を正しく満たす、最もシンプルな実装を優先する。

以下を避ける。

- 早すぎる抽象化
- 不要なアーキテクチャ層
- 過剰なコンポーネント分割
- 不要なカスタムHook
- 不要な依存パッケージ
- 将来を想定しすぎた設計
- 明確な利点のない大規模リファクタリング

同じ要件を満たすなら、複雑な実装より小さく明確な実装を優先する。

具体的な理由がない限り、正常に動作しているコードを書き直さない。

---

## 6. 変更前に行うこと

機能を実装する前に以下を行う。

1. `docs/requirements.md` の関連箇所を読む
2. `docs/screen-design.md` の関連箇所を読む
3. 現在のコードを確認する
4. 変更が既存機能へ与える影響を確認する
5. 変更が必要な最小限のファイルを特定する

大きな変更を行う場合は、実装前に短い実装計画を提示する。

現在のコードベースを理解せずに大規模な書き換えを開始しない。

---

## 7. 変更範囲

変更は目的に集中させる。

1つの機能追加に対して、無関係な改善を同時に行わない。

例：

「記録削除機能を実装する」という依頼の場合。

実施してよい：

- 削除処理の実装
- 削除エラー処理
- 関連テストの追加

同時に行わない：

- ダッシュボードの全面デザイン変更
- CSS構成の置き換え
- 認証機能の追加
- 無関係なコンポーネントのリファクタリング
- 不要なDB構造変更

変更範囲を論理的に限定すること。

---

## 8. 既存機能の保護

正常に動作している既存機能を壊さないこと。

共通コードを変更する場合は、以下への影響を確認する。

- ダッシュボード
- 記録登録
- 履歴表示
- カフェイン合計計算
- 削除
- ナビゲーション
- Supabase連携
- レスポンシブ表示

機能変更後は既存テストを実行する。

新しい機能のために、既存の正常動作を意図的に壊さないこと。

---

## 9. プロジェクト構成

基本的には、以下のようなシンプルな構成を優先する。

```text
src/
├── components/
├── pages/
├── lib/
├── App.jsx
├── App.css
└── main.jsx
```
